export const selectIsLogOutModalOpen = (state) => state.modal.isLogOutModalOpen;
export const selectIsUserModalOpen = (state) => state.modal.isUserModalOpen;
export const selectIsEditModalOpen = (state) => state.modal.isEditModalOpen;
export const selectIsAddModalOpen = (state) => state.modal.isAddModalOpen;
